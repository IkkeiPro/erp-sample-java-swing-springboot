<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ikkei.swingapp.mapper.CompositionMapper">

    <update id="createTableIfNotExists">
        CREATE TABLE IF NOT EXISTS composition (
            parent_part_no VARCHAR(100) NOT NULL,
            child_part_no VARCHAR(100) NOT NULL,
            level INTEGER NOT NULL,
            PRIMARY KEY (parent_part_no, child_part_no)
        )
    </update>

    <select id="findAll" resultType="CompositionBean">
        SELECT parent_part_no AS parentPartNo,
               child_part_no AS childPartNo,
               level
        FROM composition
        ORDER BY parent_part_no, child_part_no
    </select>

    <delete id="deleteAll">
        DELETE FROM composition
    </delete>

    <insert id="insertAll">
        INSERT INTO composition (parent_part_no, child_part_no, level)
        VALUES
        <foreach collection="rows" item="row" separator=",">
            (#{row.parentPartNo}, #{row.childPartNo}, #{row.level})
        </foreach>
    </insert>
</mapper>
